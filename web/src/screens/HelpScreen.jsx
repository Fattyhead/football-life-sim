import { useT } from '../i18n/localize.js';

/* 靜態說明卡，不做強制多步驟精靈——使用者定案：容易讓人一直按「下一步」
   跳過，不強迫看，想直接開局的人可以跳過，第一次來的人有地方可以查。
   文案語氣照抄遊戲內既有的框架文字風格(直接、口語、不說明書腔)。
   稽核修正(使用者委託：繁中/簡中雙語)：這裡每段文字中間夾了 <b> 強調
   標籤，不是單純的字串——不能整段丟給 t() 一次轉換(t() 只吃字串)，
   拆成「<b>前後的文字片段」各自單獨轉換，保留原本的粗體強調效果，
   不犧牲版面。 */
export default function HelpScreen({ onBack }) {
  const t = useT();
  return (
    <div className="app-shell">
      <h1 className="screen-title">{t('怎麼玩')}</h1>

      <div className="card">
        <div className="help-section">
          <h4>{t('一年只能做一件事')}</h4>
          <p>
            {t('每一季你要在')}
            <b>{t('訓練／機會／社交')}</b>
            {t('三個方向裡選一個——訓練練能力值，機會攢轉會籌碼跟合約談判力，社交拉人氣。時間有限，選了一個就沒空顧另外兩個，這是刻意的取捨，不是哪個方向比較「正確」。')}
          </p>
          <p>{t('戀愛線的進展(認識對象、求婚、出軌誘惑)不看你選了什麼類別，每季都會自動發生——這是人生自己會發生的事。')}</p>
        </div>

        <div className="help-section">
          <h4>{t('穩健／平衡／冒進')}</h4>
          <p>
            {t('有實質效果的選項底下還會再選一次強度：')}
            <b>{t('穩健')}</b>
            {t('成功率最高、幅度最小；')}
            <b>{t('冒進')}</b>
            {t('幅度最大，但失手會真的往下扣不少。沒有絕對正確的選法，一路走同一個風格還會疊出專屬稱號。')}
          </p>
        </div>

        <div className="help-section">
          <h4>{t('終局怎麼算')}</h4>
          <p>
            {t('退休那一刻，累積的稱號/數據會換算成一個 0-100% 的')}
            <b>{t('傳奇度')}</b>
            {t('——分數越高，代表這段人生的成色越高。90% 以上才算「傳奇」，不是隨便玩玩就摸得到，但也不是機率為零，真的花心思投入就有機會。')}
          </p>
        </div>

        <div className="help-section">
          <h4>{t('種子')}</h4>
          <p>
            {t('每一段人生都是同一顆「種子」骰出來的——同一顆種子重玩一次，會得到一模一樣的人生。想重現某一局精彩的生涯，或跟朋友比賽同一顆種子誰打得比較好，記下那串代碼就好。')}
          </p>
        </div>
      </div>

      <button className="primary-btn" onClick={onBack}>
        {t('回到標題')}
      </button>
    </div>
  );
}
